---
id: AA-001
title: tl-parse v0.1 assurance argument
type: AssuranceArgument
status: active
owner: tl-parse-release-owner
profile: ix://agent-ix/tl-parse/AP-001
top_claim:
  id: claim-v0.1-source-release
  statement: the identified tl-parse v0.1 candidate is acceptable for source release within AP-001
  subject: one identified source, dialect, dependency, corpus, and toolchain candidate
  status: open
reasoning:
  - id: reasoning-text-boundary
    statement: exact dialect mappings, fail-closed limits, round trips, and hostile-input evidence reduce uncertainty about textual-boundary correctness
    supports: claim-v0.1-source-release
    sufficiency_criteria:
      - every applicable matrix row has current passing or retained inspection evidence
      - specification, code, and gap reviews have no unresolved blocking findings
      - the human release owner accepts the named candidate revisions
assumptions:
  - id: assumption-tl-syntax
    statement: the exact pinned tl-syntax constructors enforce their published structural contract
    owner: tl-parse-release-owner
    status: confirmed
    review_by: "2027-08-30T00:00:00Z"
participants:
  - id: tl-parse-release-owner
    role: human source-release decision owner
    authority: accept or reject the identified v0.1 source candidate
    independence: reviews agent-produced implementation and evidence before deciding
challenges:
  - id: challenge-population-boundary
    target: claim-v0.1-source-release
    statement: generated and fuzz populations are bounded evidence rather than exhaustive proof over all inputs
    status: open
    owner: temporal-program-owner
  - id: challenge-temporary-git-pin
    target: claim-v0.1-source-release
    statement: the tl-syntax dependency resolves by exact git revision because that crate has no registry release; the pin is now a revision on its main branch rather than one on an open pull request
    status: open
    owner: tl-parse-release-owner
  - id: challenge-retracted-upstream-evidence
    target: claim-v0.1-source-release
    statement: resolved by repinning from 740182f1, whose upstream qualification record was retracted, to 953ee825 on tl-syntax main
    status: resolved
    owner: tl-parse-release-owner
relationships:
  - target: ix://agent-ix/tl-parse/AP-001
    type: references
---

# tl-parse v0.1 Assurance Argument

## Claim

The exact tl-parse v0.1 candidate is suitable for independent source-release
review as a deterministic, bounded textual MLTL boundary.

## Reasoning

The independently authored dialect maps one-for-one to the pinned tl-syntax
vocabulary. Fail-closed parsing, exact diagnostic goldens, bounded canonical
formatting, generated round trips, malformed corpora, and fuzz seeds address
the declared deterministic failure scenarios.

## Sufficiency Decision

The claim remains open until TM-001 is fully backed, dependency and corpus pins
are current, source and gap reviews have no blocking finding, the shared
assurance chain passes at the candidate revision, and the human owner records a
decision through ix-flow.

## Retained Evidence

Twelve records are retained under `evidence/`, the most recent for source
candidate `2b295d21fef68fb1c342a4b16e39d6fbfada098c`. Their bytes are immutable
and this argument does not restate their digests, because a digest copied into a
prose document is a second place to update and one to forget — which is what the
`ANCHORS` cross-check existed to police.

There is no longer a repository-local verifier over them. Git history and
pull-request review are the integrity boundary for retained bytes, and
`scripts/legacy_evidence_view.py` reads every envelope through the pinned
Engineering Assurance compatibility mapping without modifying one, asking Git
whether the retained bytes are the committed bytes rather than implying a
stronger claim.

That mapping's answer for all twelve is `incompatible`: they are
`quire.derivation-evidence/v1` records and the PGM-01 mapping covers only
`quire.pgm01-evidence` v1 and v2. The refusal is reported as it stands. It is
not a pass, not a defect of these records, and not a reason to write a local
mapper. Filed upstream as `agent-ix/engineering-assurance#21`.

Current verification evidence for the candidate comes from the shared assurance
chain (`make assurance`), not from a retained local envelope.

## Challenges

- Generated and fuzz populations are not exhaustive over all bounded strings.
- The parser does not establish temporal semantic correctness beyond faithful
  construction of the tl-syntax graph.
- Third-party toolchains and the pinned dependency retain their own assurance
  boundaries.
- The `tl-syntax` dependency resolves by exact git revision because that crate
  has no registry release. The earlier expiry condition — "2026-09-07 or when
  the reviewed change merges" — has been met by the merge, and the pin has moved
  from `740182f1` on a pull-request branch to `953ee825` on `main`. Source
  release remains blocked while the dependency has no registry release.
- The Make execution-control guard was removed with the local evidence
  framework. Adding `.IGNORE:` to the `Makefile` makes every recipe report
  success without running, and nothing in this repository inspects Make's own
  controls to notice. The structural backstop — Quoin binds retained inputs by
  digest and every attested result is derived from producer bytes — covers only
  the targets re-run inside `make assurance-inputs`; it does not cover
  `fmt-check`, `lint`, `test`, `check-corpus`, `fuzz-build`, `fuzz-smoke`, `deny`, `audit-unsafe`, `rustdoc`, and the `quire validate` half of `spec`. Tracked as `agent-ix/tl-parse#11`.
- Human review and release decision are still open.

Automated evidence may support but cannot close this argument; only the named
human release owner may accept residual risk and approve release.
